// const { LOGIN_ENDPOINT, REGISTER_ENDPOINT } = require("../utils/url-helper");

// const authController = require("../controllers/auth-controller");

// const Router = require("./route");

// const router = new Router();

// router.addRoute(LOGIN_ENDPOINT, authController.getLoginPage);

// router.addRoute(REGISTER_ENDPOINT, authController.getRegisterPage);

// module.exports = router.handleRoute.bind(router);
